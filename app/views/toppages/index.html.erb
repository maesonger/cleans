<% content_for :cover do %>
  <div class="cover">
    <div class="cover-inner">
      <div class="cover-contents">
        <h1>民泊物件と清掃業者マッチングサービス</h1>
          <% if logged_in? %>
            <% if current_user.is_hotel%>
              <%= link_to '清掃業者検索をする', clean_companies_search_url, class: 'btn btn-primary btn-lg' %>
            <% elsif%>
              <%= link_to '会社情報を入力する', new_clean_company_url, class: 'btn btn-primary btn-lg' %>
            <% end %>
            
          <% else %>
            <%= link_to 'Cleansを始める', signup_path, class: 'btn btn-success btn-lg' %>
          <% end %>
      </div>
    </div>
  </div>
<% end %>
